<% provide(:title, 'All users') %>
<h1>All users</h1>
<h3> Number of active users: <%= @users.count %> </h3>
<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
        <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>